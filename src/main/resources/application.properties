spring.application.name=movie-reservation-system

# MySQL Connection Details
# Logic: Use the 'DB_URL' environment variable if provided by Docker; otherwise, use localhost (for your laptop)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/movie_db?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:Akshul}
spring.datasource.password=${DB_PASSWORD:akshul@96}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true